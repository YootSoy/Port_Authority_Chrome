really shitty port to chrome that i made, since chrome does not support dns and cname endpoints this will function a bit differently

download the zip, extract the archive, go to chrome://extensions, enable developer mode, click on load unpacked and select the archive that you extracted. 
